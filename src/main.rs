// TODO:
//  Lambda 1: call to rest endpoint with location triangle -> save in dynamodb
//  Lambda 2: on cron, check if anything in dynamo - if so, do call to url and see if charger is available -> if so, send email
//  Components: lambda, cron, dynamo
//  CDK with Typescript, Rust, Github actions

fn main() {
    println!("Hello, world!");
}
